# Receipt Splitter Bot & Mini App

Кроссплатформенное решение для быстрого разделения счетов в Telegram: вы отправляете фото чека — бот распознаёт позиции (OCR), создаёт «доску» с позициями в Telegram Mini App, участники отмечают свои блюда, а приложение автоматически считает доли. Поддерживаются два OCR‑движка с переключателем: Tesseract и Yandex Vision.

## 📋 Описание проекта

Этот проект состоит из двух основных компонентов:

1. **Telegram Bot** - обрабатывает загруженные фото чеков, распознает текст с помощью OCR и создает доски для разделения счета
2. **Mini App** - веб-приложение, которое открывается внутри Telegram для управления досками, выбора позиций и расчета долей

## 🏗️ Архитектура

```
receipt-splitter-setup/
├── bot.js                 # Основной файл Telegram бота
├── ocr.js                 # OCR обработка с Tesseract.js
├── ocr_engine.js          # Движок выбора OCR (Tesseract/Yandex)
├── ocr_yandex.js          # Интеграция с Yandex Vision API
├── miniapp/               # React Mini App
│   ├── src/
│   │   ├── App.tsx        # Главный компонент приложения
│   │   ├── components/    # React компоненты
│   │   ├── lib/          # Утилиты (Supabase, Telegram)
│   │   └── types/        # TypeScript типы
│   └── public/           # Статические файлы
└── env.example           # Пример переменных окружения
```

## ⚙️ Переключатель OCR (Tesseract / Yandex Vision)

По умолчанию используется Tesseract. Чтобы включить Yandex Vision, задайте в `.env`:

```env
OCR_ENGINE=yandex   # tesseract | yandex
YANDEX_FOLDER_ID=...  # обязательен для yandex
YANDEX_API_KEY=...    # обязательен для yandex
```

После изменения переменных перезапустите бота:

```bash
pkill -f "node bot.js" || true && node bot.js
```

## 🚀 Текущее состояние

### ✅ Реализованные функции

- **OCR распознавание чеков** (Tesseract.js + Yandex Vision)
- **Создание досок** для разделения счета
- **Mini App** с интерфейсом выбора позиций
- **Real-time обновления** через Supabase
- **Расчет долей** участников
- **Режим редактирования** (скрыт для тестирования)
- **Адаптивный дизайн** для мобильных устройств

### 🔧 Технический стек

- **Backend**: Node.js, Telegram Bot API
- **Frontend**: React, TypeScript, CSS
- **Database**: Supabase (PostgreSQL)
- **OCR**: Tesseract.js, Yandex Vision API (переключатель через `OCR_ENGINE`)
- **Deployment**: Vercel (Mini App), Yandex Cloud (альтернатива)

## 📱 Развертывание Mini App в Telegram

### 1. Подготовка окружения

```bash
# Клонирование репозитория
git clone <repository-url>
cd receipt-splitter-setup

# Установка зависимостей
npm install
cd miniapp
npm install
cd ..
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `env.example`:

```bash
cp env.example .env
```

Заполните необходимые переменные:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
MINIAPP_URL=https://your-miniapp-url.vercel.app

# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Yandex Vision (опционально)
YANDEX_FOLDER_ID=your_folder_id
YANDEX_API_KEY=your_api_key
OCR_ENGINE=tesseract  # или yandex
```

### 3. Настройка базы данных Supabase

1. Создайте проект в [Supabase](https://supabase.com)
2. Выполните SQL скрипты для создания таблиц:

```sql
-- Создание таблиц
CREATE TABLE profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  telegram_id BIGINT UNIQUE,
  first_name TEXT,
  last_name TEXT,
  username TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE boards (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  created_by UUID REFERENCES profiles(id),
  restaurant_name TEXT,
  address TEXT,
  total_amount DECIMAL(10,2),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE participants (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  board_id UUID REFERENCES boards(id) ON DELETE CASCADE,
  profile_id UUID REFERENCES profiles(id),
  name TEXT NOT NULL,
  is_creator BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE bill_items (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  board_id UUID REFERENCES boards(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  price DECIMAL(10,2) NOT NULL,
  quantity INTEGER NOT NULL,
  emoji TEXT DEFAULT '🍽️',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE item_selections (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  participant_id UUID REFERENCES participants(id) ON DELETE CASCADE,
  item_id UUID REFERENCES bill_items(id) ON DELETE CASCADE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(participant_id, item_id)
);
```

3. Настройте RLS (Row Level Security) политики

### 4. Развертывание Mini App

#### Вариант A: Vercel (рекомендуется)

```bash
# Установка Vercel CLI
npm i -g vercel

# Вход в аккаунт
vercel login

# Развертывание
cd miniapp
vercel --prod

# Получите URL и обновите MINIAPP_URL в .env
```

#### Вариант B: Netlify

```bash
# Установка Netlify CLI
npm i -g netlify-cli

# Развертывание
cd miniapp
npm run build
netlify deploy --prod --dir=build

# Получите URL и обновите MINIAPP_URL в .env
```

#### Вариант C: Yandex Cloud (рекомендуется для РФ)

1) Создайте S3-бакет в Yandex Object Storage (включите Public access или подключите CDN)
2) Включите «Static website hosting»: Index document = `index.html`, Error document = `index.html`
3) Создайте сервисные ключи (Access key / Secret key)
4) Заполните переменные в `.env`:

```env
YC_S3_ENDPOINT=https://storage.yandexcloud.net
YC_S3_REGION=ru-central1
YC_S3_BUCKET=<имя_вашего_бакета>
YC_S3_ACCESS_KEY_ID=<access_key>
YC_S3_SECRET_ACCESS_KEY=<secret_key>
```

5) Установка зависимостей для загрузки:

```bash
npm i -D @aws-sdk/client-s3 mime
```

6) Сборка и загрузка Mini App:

```bash
cd miniapp && npm run build && cd ..
node scripts/yc-upload.js
```

7) Возьмите публичный endpoint бакета/сайта и установите его в `MINIAPP_URL` в `.env`, затем перезапустите бота.

### 5. Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Настройте Mini App:

```
/setmenubutton
@your_bot_username
Открыть Mini App
https://your-miniapp-url.vercel.app
```

4. Настройте команды бота:

```
/setcommands
@your_bot_username
start - Запустить бота
help - Помощь
```

### 6. Запуск бота

```bash
# Запуск в продакшене
node bot.js

# Или с PM2 для автозапуска
npm install -g pm2
pm2 start bot.js --name "receipt-splitter-bot"
pm2 save
pm2 startup
```

## 🔧 Разработка

### Локальная разработка

```bash
# Запуск Mini App в режиме разработки
cd miniapp
npm start

# В другом терминале - запуск бота
cd ..
MINIAPP_URL=http://localhost:3000 node bot.js
```

### Тестирование

```bash
# Тест подключения к Supabase
node test-supabase.js

# Тест загрузки доски
node test-board-load.js
```

## 📱 Использование

1. **Отправка чека**: Пользователь отправляет фото чека боту
2. **OCR обработка**: Бот распознает текст и извлекает позиции
3. **Создание доски**: Создается доска с позициями и общей суммой
4. **Открытие Mini App**: Пользователь открывает Mini App для управления
5. **Выбор позиций**: Участники выбирают свои позиции
6. **Расчет долей**: Автоматический расчет долей каждого участника

## 🎯 Основные функции Mini App

- **Просмотр позиций** с ценами и количеством
- **Выбор позиций** одним кликом
- **Real-time обновления** при изменениях
- **Расчет итоговой суммы** для каждого участника
- **Режим редактирования** (для создателя доски)
- **Адаптивный дизайн** для мобильных устройств

## 🐛 Известные проблемы

1. **OCR точность**: Зависит от качества фото и типа чека
2. **Кэширование**: Иногда требуется очистка кэша браузера
3. **Права доступа**: Только создатель доски может редактировать

## 🔄 Обновления

### Последние изменения

- ✅ Улучшен формат отображения расчетов
- ✅ Исправлены предупреждения ESLint
- ✅ Добавлен режим редактирования (скрыт)
- ✅ Улучшена адаптивность интерфейса

### Планы развития

- [ ] Улучшение OCR точности
- [ ] Добавление истории чеков
- [ ] Экспорт результатов
- [ ] Мультиязычность
- [ ] Уведомления участников

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи бота в терминале
2. Убедитесь, что все переменные окружения настроены
3. Проверьте подключение к Supabase
4. Очистите кэш браузера

## 📄 Лицензия

MIT License

---

**Статус проекта**: ✅ Рабочая версия  
**Последнее обновление**: Январь 2025  
**Версия**: 1.0.0